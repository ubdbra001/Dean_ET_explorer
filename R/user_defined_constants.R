# default AOI properties
Left_x = c(0, 0.4)
Left_y = c(0, 1)
Right_x = c(0.6, 1)
Right_y = c(0, 1)

# Slider bar properties
lower_limit = 0
upper_limit = 1

step_size = 0.01


# Constants for plotting
hjust = 1.1
vjust = 1
y_lims = c(0, 1)

# Sample rate for calculating sample time
sample_rate = 120

# Default for look length
look_length_default = 12